# Programmatic Video — poster

Source artboards for the Programmatic Video poster.

| File | What it is |
|---|---|
| `Main.dc.html` | The poster. A3 at 96 px/inch (1123×1587). |
| `DirectionInk.dc.html` | Alternate direction, low-fi: paper white, photocopy-safe. |
| `DirectionSlate.dc.html` | Alternate direction, low-fi: headline inside a 16:9 frame. |
| `canvas.json` | Canvas layout + notes on each direction's tradeoff. |

Palette is lifted from `assets/hero.gif`'s color table rather than invented —
ground `#06080f`, accent `#42b7f7`, violet `#7d6df6`, text `#fafbfb`.
Type is Archivo Black / Archivo / IBM Plex Mono.

## Open items

Three fields on the poster are bracketed placeholders, not copy:
`[FORMATS & RESOLUTION]`, `[TYPICAL RENDER TIME]`, `[API / UPLOAD / CLI]`.
The four pipeline beats are drafted from the estate README's one-line
description of this product and should be checked against what it does.
